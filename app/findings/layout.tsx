/** The room itself is a client component, so its title lives here. */
export const metadata = {
  title: 'Three Answers — Second Nature, Art House NMACC',
  description:
    'What we observed, why we think the brand designed the experience this way, and what the visit changed about our understanding of luxury.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
